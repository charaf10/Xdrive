
    //from: 'azineddine.stockage@gmail.com',

// emailUtils.mjs
/*
import nodemailer from 'nodemailer';

const transporter = nodemailer.createTransport({
  service: 'gmail',
  auth: {
    user: 'azineddine.stockage@gmail.com',
    pass: 'Xxyyzz10!',
  },
});

const sendWelcomeEmail = (driverEmail) => {
  const mailOptions = {
    from: 'azineddine.stockage@gmail.com',
    to: driverEmail,
    subject: 'Bienvenue sur notre application',
    text: 'Bienvenue ! Merci de vous être inscrit sur notre application.',
  };

  transporter.sendMail(mailOptions, (error, info) => {
    if (error) {
      console.error(error);
    } else {
      console.log('E-mail envoyé : ' + info.response);
    }
  });
};

export { sendWelcomeEmail };
*/